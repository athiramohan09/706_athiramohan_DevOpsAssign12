pipeline {
  agent any
  stages {
    stage('Build') {
      steps { echo 'Building Docker image (placeholder)' }
    }
    stage('Deploy') {
      steps { echo 'Deploying stack (placeholder)' }
    }
  }
}
